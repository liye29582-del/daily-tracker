# 日常记录软件 · PRD（P1 核心闭环）

| 项 | 内容 |
|----|------|
| 文档版本 | v1.0（评审稿） |
| 阶段 | 阶段二：PRD 撰写 |
| 范围 | P1 核心闭环（体重 / 待办 / 番茄钟 / 日历 / 统计 / 设置） |
| 关联文档 | `日常记录软件-策划案.md`（需求规划） |
| 最后更新 | 2025 |

> 需求描述遵循 **EARS 原则**，每条标注类型：
> **U**=Ubiquitous（始终满足）｜**E**=Event-driven（事件触发）｜**W**=Unwanted（异常处理）｜**S**=State-driven（状态相关）｜**O**=Optional（可选功能）

---

## 1. 背景

用户当前需用多个独立 App 分别管理体重、待办与番茄钟，切换成本高、数据割裂。本产品面向学生 / 职场新人，提供一个**本地优先、免登录**的轻量聚合入口，先跑通「记录 → 统计」核心闭环验证价值。详细背景与竞品判断见策划案。

## 2. 目标与成功指标

**产品目标（P1）**：交付可日常使用的核心记录闭环，验证「聚合式日常记录」对用户留存与效率的价值。

| 指标 | 口径 | 目标值（建议） |
|------|------|----------------|
| 周活跃天数 | 用户每周打开并记录的的天数 | ≥ 3 天（验证期） |
| 体重记录留存率 | 连续记录 ≥7 天用户占比 | 观察项 |
| 待办完成率 | 已完成 / 已创建 | 观察项 |
| 日均番茄数 | 番茄钟完成数 | 观察项 |

## 3. 用户故事

- **作为减脂期学生**，我希望每天称完体重顺手记录并看到趋势曲线，以便了解体重变化。
- **作为备考职场新人**，我希望把任务列成待办并用番茄钟专注，以便提升完成率与专注时长。
- **作为自律养成者**，我希望在首页一眼看到今日概览，以便快速进入记录状态。

## 4. 功能清单

| 模块 | 功能要点 | 优先级 |
|------|----------|--------|
| 全局 | 本地存储（IndexedDB）、响应式布局、首次引导、数据导出 | P0 |
| 体重记录 | 录入（数值+日期+备注）、趋势曲线、目标线、删除 | P1 |
| 每日待办 | 增删改、完成切换、今日/全部筛选、分类标签 | P1 |
| 番茄钟 | 计时（专注/休息）、绑定待办、累计专注时长、环形进度 | P1 |
| 日历 | 月视图聚合标记、当日明细 | P1 |
| 统计 | 体重趋势、近 14 天专注、待办完成率、累计专注 | P1 |
| 设置 | 番茄/休息时长、目标体重、导出 | P1 |

---

## 5. 需求详述（EARS）

### 5.1 全局 / 本地存储

- **FR-001（U）** 系统应始终将用户所有业务数据保存在**本机浏览器（IndexedDB）**，不上传任何服务器。
- **FR-002（U）** 系统应始终在首次启动时展示「欢迎 / 引导」弹窗，用户点击「开始使用」后方可进入主界面。
- **FR-003（E）** 当用户点击引导弹窗的「开始使用」时，系统应将 `onboarded` 标记为 true，且后续启动不再展示该弹窗。
- **FR-004（U）** 系统应始终在宽屏展示左侧导航栏、在窄屏（移动端）展示底部 Tab 导航，保证响应式可用。
- **FR-005（W）** 若浏览器不支持 / 禁用了 IndexedDB，则系统应在进入主界面时提示「当前环境无法保存数据」，并仅提供本次会话内的只读展示。
- **FR-006（O）** 对于「数据导出」功能，系统应提供 JSON（全量）与 CSV（体重）两种格式供用户留存或换设备。

### 5.2 体重记录

- **FR-101（E）** 当用户输入体重数值并点击「保存记录」时，系统应将一条体重记录（日期、数值、可选备注、创建时间）写入本地库，并刷新体重页与首页展示。
- **FR-102（U）** 系统应始终以 **kg** 为体重单位，录入支持一位小数。
- **FR-103（S）** 在体重记录数 **≥ 2** 时，系统应在体重页展示趋势折线图；不足 2 条时仅展示列表。
- **FR-104（O）** 若用户在设置中配置了「目标体重」，系统应在趋势图上以红色虚线展示目标参考线。
- **FR-105（W）** 若用户输入非正数值或留空，则系统应阻止提交并提示「请输入有效体重」。
- **FR-106（E）** 当用户点击某条体重记录的「删除」时，系统应移除该记录并刷新列表与图表。
- **FR-107（U）** 系统应始终按日期升序存储体重记录，列表默认按时间倒序展示。

### 5.3 每日待办

- **FR-201（E）** 当用户填写标题并点击「添加待办」时，系统应创建一条待办（标题、日期、可选标签、未完成状态）。
- **FR-202（E）** 当用户勾选待办的复选框时，系统应切换其完成状态并刷新完成率统计。
- **FR-203（E）** 当用户点击待办「删除」时，系统应移除该待办。
- **FR-204（S）** 在「今日」筛选下，系统应仅展示日期等于当天的待办；在「全部」下展示全部待办。
- **FR-205（U）** 系统应在每条待办上展示其计划日期、标签（如有）及累计专注时长（如有）。
- **FR-206（W）** 若用户提交空白标题，则系统应阻止创建并提示「请输入内容」。

### 5.4 番茄钟

- **FR-301（U）** 系统应始终默认按设置中的「专注时长 / 休息时长」（默认 25 / 5 分钟）计时。
- **FR-302（E）** 当用户点击「开始」时，系统应启动倒计时并进入 running 状态；点击「暂停」应暂停；点击「重置」应回到当前模式的初始时长。
- **FR-303（S）** 在「专注」模式下，系统应展示可选「绑定待办」下拉（仅含当日未完成待办）。
- **FR-304（E）** 当一段专注计时自然结束（remaining 归零）时，系统应写入一条 `focus` 类型的 PomodoroSession，并若该次绑定了待办，则将该段时长累加至该待办的 `focusSeconds`。
- **FR-305（E）** 当一段休息计时结束时，系统应切回「专注」模式并停止，等待用户再次开始。
- **FR-306（O）** 对于绑定的待办，系统应在其专注结束后于待办项展示累计专注时长（格式：X小时Y分 / X分）。
- **FR-307（S）** 在计时 running 状态下且用户处于非番茄钟页面时，系统应在顶部（移动端）展示进行中浮标（模式 + 剩余时间），点击可跳转回番茄钟页。
- **FR-308（W）** 若用户在执行中刷新页面，则系统应清空当前计时状态（不记录未完成的片段），并以设置中的默认时长重新初始化计时器。

### 5.5 日历

- **FR-401（U）** 系统应始终以月视图展示日历，支持上 / 下月切换，周日起始。
- **FR-402（S）** 对于有数据的日期，系统应在日期格上以圆点标记该日是否存在体重（蓝）/ 待办（绿）/ 专注（黄）。
- **FR-403（E）** 当用户点选某日期时，系统应在下方展示该日明细：体重数值、待办数量与完成数、专注个数。
- **FR-404（U）** 系统应始终高亮「今天」日期。

### 5.6 统计

- **FR-501（U）** 系统应始终展示三项汇总卡片：总待办数、待办完成率、累计专注时长。
- **FR-502（S）** 在体重记录 ≥ 2 条时，系统应展示体重趋势折线图；否则展示占位提示。
- **FR-503（U）** 系统应始终展示「近 14 天专注（分钟）」柱状图，按天聚合 focus 类型 session 的时长（分钟）。
- **FR-504（U）** 完成率口径应始终为 `已完成待办数 / 总待办数`（含历史）。

### 5.7 设置 / 导出

- **FR-601（E）** 当用户修改「专注时长 / 休息时长 / 目标体重」并触发保存时，系统应持久化设置，并即时影响番茄钟计时与体重目标线。
- **FR-602（E）** 当用户点击「导出全部 (JSON)」时，系统应下载包含 weights / todos / pomodoros / settings 的 JSON 文件。
- **FR-603（E）** 当用户点击「导出体重 (CSV)」时，系统应下载含 date,value,note 的 CSV 文件。

---

## 6. 流程说明

1. **记录体重**：体重页/首页 → 输入数值+日期(+备注) → 保存 → 列表/曲线/首页更新。
2. **完成待办**：待办页 → 新建 → 勾选完成 → 完成率+1。
3. **番茄钟绑定任务**：Focus 页 → 选待办 → 开始 → 结束自动记 session 并累加任务时长。
4. **查看日历**：日历页 → 切换月份 → 点日期 → 看当日明细。
5. **导出备份**：设置页 → 选格式 → 下载文件。

## 7. 交互说明

- 导航：宽屏左侧栏、窄屏底部 Tab；当前页高亮。
- 表单：输入即时聚焦，错误内联提示，不弹窗阻断。
- 番茄钟：环形进度 + 大号计时；专注/休息模式切换为分段控件。
- 反馈：保存/删除后列表即时刷新；无 toast 重载。

## 8. 边界与异常场景

| 场景 | 预期处理 |
|------|----------|
| 体重记录为空 | 列表显示空态文案；趋势图隐藏 |
| 同一天多条体重 | 允许；趋势图按日期取该日最后一条（或全展示点）|
| 待办标题为空 | 阻止创建，提示 |
| 体重值非法（≤0/空） | 阻止提交，提示 |
| 番茄钟进行中刷新页面 | 清空计时，不记录未完成片段（见 FR-308）|
| IndexedDB 不可用 | 提示无法保存，仅本次会话只读 |
| 绑定待办在专注中被删除 | 该 session 仍记录，但 `focusSeconds` 累加因 todoId 失效而跳过（已知边界）|
| 导出时无任何数据 | 仍生成空结构文件，不报错 |

## 9. 权限规则

- **单人、本地、无账号**：无登录、无多用户、无角色权限差异。
- 所有数据归属当前浏览器，清除浏览器数据即丢失（除非已导出）。

## 10. 数据口径

- 体重单位：kg，保留一位小数。
- 专注时长：`PomodoroSession.duration` 累加（秒）→ 展示时换算为「X小时Y分 / X分」。
- 完成率：`done / total`（全量待办）。
- 近 14 天专注：按本地日期（`new Date(start).toDateString()`）聚合 focus 且 done 的 session。
- 目标线：取设置中的 `targetWeight`。

## 11. 埋点需求（本地）

| 事件 | 触发 | 用途 |
|------|------|------|
| module_view | 进入某页面 | 功能使用分布 |
| weight_add / todo_add / pomo_done | 对应动作完成 | 活跃度 / 专注价值 |
| export_click | 点击导出 | 备份需求强度 |

> 埋点数据存本地，用于自评估；后续接分析后台再上报。

## 12. 验收标准

- [ ] 体重录入后，体重页与首页即时更新；≥2 条出现趋势图。
- [ ] 待办可增删改、勾选完成并反映在完成率。
- [ ] 番茄钟可开始/暂停/重置；专注结束写入 session 并（若绑定）累加待办时长。
- [ ] 日历月视图正确标记并展示当日明细。
- [ ] 统计三项汇总与两张图表数据正确。
- [ ] 设置修改即时生效；导出 JSON / CSV 文件可正常下载。
- [ ] 首次启动展示引导，再次启动不展示。
- [ ] 窄屏底部 Tab、宽屏侧栏均可导航。
- [ ] 空数据、非法输入、刷新重置等边界符合第 8 节预期。

## 13. 非功能需求

- 兼容性：现代浏览器（Chrome/Edge/Safari/Firefox 近两版）。
- 性能：本地读取，首屏 ≤ 1s 可见；图表按需渲染。
- 隐私：数据不出本机。
- 响应式：适配手机浏览器（PWA 化留待后续）。

## 14. 待确认 / 开放问题

1. **提醒能力**：P1 仅应用内；是否 P2 接浏览器 Notification（需授权）？
2. **番茄钟保活**：刷新重置是否可接受？还是需后台保活？
3. **体重同日多值**：取末值还是保留多点？
4. **导出范围**：是否需要 CSV 全量（含待办/番茄）而不仅是体重？
5. **视觉风格**：清新简约 / 仪表盘风 / 拟物可爱？（影响设计稿）

---

*本 PRD 覆盖 P1 核心闭环，供技术负责人、设计师、测试评审。P2（习惯打卡/心情/喝水/提醒）另行补充。*
