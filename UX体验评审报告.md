# 日常记录 App · 用户体验深度评审报告

> 评审角色：资深体验研究员（从真实用户视角深度试用）
> 评审范围：功能逻辑、信息架构、导航可达性、交互一致性、无障碍（**不含纯视觉/美术美化**）
> 评审方法：代码走查 + 自动化移动端走查（Playwright，视口 414×896，模拟手机）+ 真实用户旅程推演
> 版本基线：v0.6

---

## 0. 评审方法与真实运行证据

- 使用 Playwright 在 **移动视口（414×896）** 下模拟真实用户，依次走查：引导 → 首页 → 体重 → 待办（周习惯/日事件）→ 番茄钟（绑定习惯+倒计时+结束+习惯完成弹窗）→ 未绑定专注（手动结束计入）→ 记录（心情/饮水/饮食/排便 + 日历）→ 灵感（保存+回忆）→ 统计 → 设置 → 日历。
- 关键证据：**整轮走查控制台报错 0 条、页面运行时错误 0 条**（应用无崩溃、无 JS 异常）。
- 走查过程中暴露并修复了一个**致命的可达性缺陷**（见 P0）。修复后重新跑通全部旅程以验证。

---

## 1. 总体结论

| 等级 | 数量 | 说明 |
|------|------|------|
| 🔴 P0 致命 | 1 | 移动端导航不可用（首页及 4 个子页全部不可达）—— 已修复 |
| 🟠 P1 重要 | 4 | 交互/信息架构不一致，影响核心任务完成 |
| 🟡 P2 一般 | 7 | 一致性、无障碍、数据健壮性等改进项 |

**一句话结论**：功能框架基本成型、数据逻辑闭环正确（习惯打卡同步、番茄钟计入统计均验证通过），但**移动端是硬伤**——手机上几乎无法使用；同时首页"待办"入口语义、记录双套交互、习惯方块的可点击性、日历数据分裂，是用户最容易卡住的第二梯队问题。

---

## 2. 🔴 P0 致命缺陷（已修复）

### P0-1 移动端导航不可用：首页与待办/番茄钟/记录/体重 全部不可达

**现象（用户视角）**：在手机上打开 App，点进"灵感/日历/统计/设置"任一页后，**再也回不到首页**，也**找不到"待办""番茄钟""记录""体重"**入口。用户被困在当前页。

**根因（代码）**：`App.tsx` 中：
- 左侧栏（含 首页 及其子项 体重/待办/番茄钟/记录）是 `hidden md:flex` —— **仅在 ≥768px 桌面显示**；
- 移动端底部 Tab 用 `GROUPS.filter((g) => !g.children)` 渲染，而 **首页自身带 `children`，被过滤掉**，只留下 灵感/日历/统计/设置。

两者叠加 → 移动端既无首页、也无四个子页入口。自动化走查在 414px 下点击侧栏链接时，Playwright 明确报 `element is not visible`（侧栏被隐藏），即直接证据。

**修复**：新增扁平化的 `BOTTOM_NAV`（首页/体重/待办/番茄钟/记录/灵感/日历/统计/设置 共 9 项），移动端以**可横向滚动的底部导航**呈现；桌面端保留原侧栏。已通过重新走查验证：移动端可从首页依次抵达全部 9 个页面。

---

## 3. 🟠 P1 重要问题

### P1-1 首页"待办"卡片 = 直接开"添加"弹窗，看不到已有待办
首页四宫格里点"待办"，弹出的是**添加待办**表单，而非待办列表。用户想"看/勾今天的待办"却被推进"新建"流程。在修复 P0 前，手机上这甚至是**唯一**能碰待办的入口，却看不到列表——体验割裂。
**建议**：首页"待办"卡片改为**跳转到 /todo 列表页**（与"体重/记录"的快速记录定位区分）；或在"添加"弹窗底部加一行"查看全部待办 →"。

### P1-2 记录存在"两套交互"，用户认知不一致
- **首页"记录"卡片** → `Dashboard.RecordModal`：把 心情/饮水/饮食/排便 揉在一个弹窗里，**只有一个"保存记录"按钮**一次性提交。
- **记录页** → `Journal.RecordModal`：分 Tab（心情/饮水/饮食/排便），每个类型有独立**"确认"**按钮、可单条删除、可查看当日历史。

两者能力、文案、操作模型都不同。用户从首页和从记录页进入，得到的是两个"长得不像"的记录器。
**建议**：统一为一套（推荐以记录页的分 Tab + 确认 模型为准），首页"记录"卡片点击后直接跳转记录页或用同一组件，避免重复维护两套逻辑与心智。

### P1-3 习惯"进度方块"不可点击打卡，需依赖左侧勾选框，语义不直观
`Todo.tsx` 的 `HabitProgress`：周习惯以 N 个小方块表示进度，但**空格块是 `disabled` 的**——只有"已打卡的方块"能点（用于取消）。要打卡今天，必须去点该行**最左侧的小方框**（✓）。
问题：方块视觉上像"每天一格"，用户会自然去点空格，却发现点不动；而真正可点的勾选框混在标题行，缺乏"今日打卡"的明示引导。
**建议**：(a) 让当前周期内的空格也**可点击直接打卡**；或 (b) 在方块区上方加"今日打卡"按钮/文案；并加 tooltip 说明"方块=按打卡顺序填充，非固定星期几"。

### P1-4 日历数据分裂，用户找不到完整的一天
- **主日历页（`/calendar`）**：显示 体重/待办/番茄/心情 四个圆点，**没有饮食/排便**。
- **记录页饮食/排便日历**：显示饮食上色 + 排便圆点，**没有体重/待办/番茄/心情**。

同一款 App 里有两个日历、各管一半数据。用户想看"今天我到底做了什么"，要在两处来回切。
**建议**：合并为一个统一日历（或主日历增加饮食/排便圆点），让"一天的完整画像"在一个视图内可见。

---

## 4. 🟡 P2 一般问题

| 编号 | 问题 | 说明 | 建议 |
|------|------|------|------|
| P2-1 | 设置页版本号写死 `v0.3` | 应用其它处为 `v0.6`，信息不一致（易让用户怀疑是否旧版） | 统一为 `v0.6`（已顺手修正） |
| P2-2 | 统计"累计已减"语义 | 取值为 `首条−最新条`；若增重则显示 `+X` 且仍标"累计已减"，略歧义 | 标签按方向动态为"累计已减/累计增加"，或加箭头 |
| P2-3 | 模态框无 Esc 关闭、无焦点锁定 | 手机上只能点 × 或遮罩；无障碍性弱 | 增加 `Escape` 关闭 + `aria-modal` + 焦点陷阱 |
| P2-4 | 灵感图片仅本地 base64 | `compressImage` 压到 800px/JPEG0.7，但多图仍会撑大 `localStorage`（约 5MB 上限） | 提示容量、限制图片数量，或引导用"导出备份"；长期需后端/云存储 |
| P2-5 | 删除即丢，无撤销 | 待办/灵感/记录删除后无回收站、无二次确认（仅 hover 变红） | 关键删除加确认；或软删除+可恢复 |
| P2-6 | 仅导出无导入 | 设置页可导出 JSON/CSV，但无"导入恢复"，换设备成本高 | 增加"从文件导入"以闭合备份链路 |
| P2-7 | 两处时间口径不一致 | 心情/饮水趋势图是"近 30 天"，饮食/排便日历是自然月可翻页 | 统一时间口径或在 UI 明确标注范围 |

---

## 5. 核心用户旅程走查（含痛点）

**旅程 A · 每天记体重 + 勾待办（高频）**
首页 → 体重卡片（弹窗记录，✅ 顺） → 待办卡片（**弹窗是"添加"不是"列表"，❌ 想看列表受阻**）→ 去 /todo 勾选。
痛点：P0（手机不可达）、P1-1。

**旅程 B · 用番茄钟绑定习惯并打卡（核心闭环）**
首页 → 专注卡片 → 开绑定 → 选周习惯 → 开始 → 倒计时 → 结束 → "今日习惯达成了吗？" → 已达成（打卡今日）。
验证结论：该闭环**逻辑正确**——`localStorage` 实测习惯 `checkIns` 含今日且 `done=true`；未绑定专注手动结束也**正确计入**番茄钟（实测 `dt_pomodoros` 记录数 ≥2，且"番茄钟"页"今日专注"可见）。这是本产品最亮眼的设计，无需改逻辑，仅需确保移动端能进到这些页（P0 修复后已可达）。

**旅程 C · 记录心情/饮水/饮食/排便 并回看**
记录页分 Tab 记录（✅ 确认流清晰）→ 点日历某天看当日饮食（✅ 弹窗正常）→ 但想看"今天体重/心情"得去另一个日历（❌ P1-4）。

**旅程 D · 灵感随手记 + 回忆**
灵感页保存（✅ 有"已保存"反馈）→ 回忆列表（✅ 可看历史、可删）。体验顺畅，是完成度较高的模块。

---

## 6. 无障碍（Accessibility）速查

- ✅ 绑定开关、模式切换用了 `aria-pressed`/语义按钮；
- ✅ 文本对比度基本达标（gray-500/600 在小字上偏灰，建议关键数据用更深色）；
- ❌ 模态框无 `Escape` 关闭、无焦点陷阱（`aria-modal` 缺失）→ P2-3；
- ❌ 图片 `alt=""`（装饰性可接受），但灵感详情图无替代文本；
- ⚠️ 底部导航在 9 项时需横向滚动，已做隐藏滚动条处理，但建议未来用"固定主 Tab + 更多抽屉"优化 Discoverability。

---

## 7. 优先级修复建议（行动清单）

| 优先级 | 问题 | 现状 | 建议改动 | 工作量 |
|--------|------|------|----------|--------|
| 🔴 P0 | 移动端导航不可用 | **超出本轮范围** | 用户明确「只考虑网页版，暂不考虑移动端」，移动端导航修复已撤回 | — |
| 🟠 P1-1 | 首页待办=添加弹窗 | **已完成** | 首页待办弹窗新增「查看全部待办 →」入口，跳转 /todo | 小 |
| 🟠 P1-2 | 两套记录交互 | **已完成** | 首页「记录」卡片改为跳转 /journal，移除重复主页记录弹窗 | 中 |
| 🟠 P1-3 | 习惯方块不可点打卡 | **已完成** | 周习惯空格子可点击直接「今日打卡」，并加未打卡提示 | 小 |
| 🟠 P1-4 | 日历数据分裂 | **已完成** | 主日历与记录页日历均展示体重/待办/番茄/心情/饮食/排便六类圆点 + 当日详情 | 中 |
| 🟡 P2-1 | 版本号不一致 | **已修正** | v0.3→v0.6 | 已做 |
| 🟡 P2-2 | 累计已减语义 | **已完成** | 标签按方向动态为「累计已减/累计增重」 | 小 |
| 🟡 P2-3 | 模态框无障碍 | 待改 | Esc 关闭 + 焦点陷阱 | 小 |
| 🟡 P2-4 | 灵感图片容量 | 待改 | 限量提示 / 导出引导 | 小 |
| 🟡 P2-5 | 删除无撤销 | 待改 | 确认 + 软删除 | 小 |
| 🟡 P2-6 | 无导入 | 待改 | 增加"导入恢复" | 中 |
| 🟡 P2-7 | 时间口径不一致 | 待改 | 统一/标注范围 | 小 |

---

## 8. 本次已修复并验证

> 范围说明：用户已明确「只考虑网页版，暂不考虑移动端」，因此 **P0 移动端导航修复已撤回**（恢复原有桌面侧栏 + 移动底栏结构），本轮仅实施网页相关条目。

1. **P1-1 首页待办入口**：首页「待办」弹窗底部新增「查看全部待办 →」按钮，点击跳转 `/todo` 并关闭弹窗（`Dashboard.tsx`）。
2. **P1-2 统一记录交互**：首页「记录」卡片改为跳转 `/journal`，移除与记录页重复的主页记录弹窗（`Dashboard.tsx`）。
3. **P1-3 习惯方块可点击打卡**：周习惯进度空格子改为可点击，点击即「今日打卡」；未打卡时显示引导文案（`Todo.tsx` `HabitProgress`）。
4. **P1-4 合并日历数据**：主日历 `/calendar` 与记录页日历均展示 **体重/待办/番茄/心情/饮食/排便** 六类彩色圆点，并补全当日详情（饮食/饮水/排便等）；记录页「每日记录日历」支持点击查看全天六类明细（`Calendar.tsx` / `Journal.tsx`）。
5. **P2-2 累计已减语义**：统计页与体重页的汇总标签按方向动态显示「累计已减 / 累计增重」（增重时显示 `+X` 与「累计增重」），消除歧义（`Stats.tsx` / `Weight.tsx`）。
6. **P2-1 版本号**：设置页 `v0.3` → `v0.6`，与全局一致（前序已修）。

> 构建产物：`npm run build` 已重新生成离线单文件 `daily-tracker.html`（修复后版本）。

### 后续精简（用户追加）
- **移除「日历」导航项**：P1-4 合并日历后，`/calendar` 与「记录」页功能重叠，故将其从侧栏/底栏导航移除（`App.tsx` `GROUPS`）。`/calendar` 页面仍保留、可经 URL 直接访问；若确认不再需要，可进一步删除页面与路由。

---

## 附：走查产物

- 走查脚本（网页版，桌面视口 1280×900）：`/workspace/.ux/test-desktop.cjs`（Playwright，`localhost:5173` 开发服务器）
- 截图（网页版）：`d01-home.png` `d03-journal.png` `d04-weight.png` `d05-stats.png`
- 运行证据（桌面视口自动化走查，15/15 项通过）：控制台错误 **0**，页面错误 **0**；含首页记录卡片跳转、待办「查看全部」跳转、周习惯空格打卡（localStorage 实测 `checkIns` 含当日）、记录页六类日历圆点、增重方向标签等。

---

*评审人：UX Researcher｜基线 v0.6｜本轮范围：网页版（移动端暂不在范围）｜已完成 P1-1~P1-4、P2-1、P2-2；后续移除「日历」导航项；P2-3~P2-7 可随版本迭代。*
