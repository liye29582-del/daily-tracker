# 日常记录软件 · 访问与预览说明

## 推荐方式（永久·离线）：双击 HTML 即开

> **`/workspace/daily-tracker.html`**（单文件，约 610 KB）

- 在 CodeBuddy 工作区找到 `daily-tracker.html` → 下载到本机 → 双击用浏览器打开。
- **无需联网、无需服务器、不会过期**。
- 数据存储已改为 `localStorage`，可正常在 `file://` 下运行（此前 `file://` 打不开是因为 Chrome 拦截了 IndexedDB）。
- 数据仅存于本机浏览器，清空浏览器数据即丢失；设置页「导出全部 (JSON)」可备份。

## 备用方式（临时·在线预览）

> Cloudflare 临时隧道链接（每次重启会变，沙箱空闲会被回收，可能随时失效）

当前链接示例：`https://require-deny-filed-wan.trycloudflare.com`
> 以实际最新给出的地址为准。失效后告诉我，我重新拉起隧道即可。

## 功能范围（v0.6）

- **首页**：居中「今日概览」+ 四张小卡片（体重 / 待办 / 番茄钟 / 记录），点卡片直接弹窗记录，数据同步到对应板块。
- **首页添加待办**：支持 每日 / 每周 / 每月，周 / 月 可再选「事件 / 习惯」并填目标天数（周默认 5 天、月默认 20 天）。
- **番茄钟（细化修改4 修复）**：无论是否绑定待办，**手动点「结束」也会记录本次专注**（修复了此前手动结束不计入番茄钟的 bug），并实时同步到「今日专注 / 近期专注图 / 专注热力」。
- **习惯进度**：每周=方框（按打卡顺序填充）；每月=进度条（打卡增长，达标 100%）。
- **记录页（细化修改4）**：
  - 心情：按钮改为「确认」，心情趋势**记录 1 天即可展示**。
  - 饮水：+杯 先暂存，点「确认」才保存；喝水统计**记录 1 天即可展示**。
  - 新增**饮食 / 排便日历**（可翻月）：有饮食的日期上色，有排便的日期下方显示圆点；点某天弹窗看当天饮食（及饮水/排便）。
- **灵感随心记（细化修改4，新增导航项💡）**：记录突发灵感 / 日记，含 日期、时间、标题、内容、可插入图片（本地压缩存储）；右上角「回忆」按钮随时查看历史记录，点单条看详情，可删除。
- **统计**：本周 / 本月 切换下三张卡片（累计已减 / 完成待办 / 累计专注）+ 完成率进度条。
- 导航：首页（可展开 4 个下级模块）· 日历 · 统计 · 灵感 · 设置（同级的五项）。
- 风格：小清新薄荷绿 + 柔和渐变背景 + 大圆角卡片。

## 链接失效的处理

1. 优先改用离线文件 `daily-tracker.html`（最稳）。
2. 仍想用在线链接 → 让我重启隧道，会给你一个新地址。
3. 想要永久公网地址 → 提供 GitHub / Netlify / Vercel 任一部署凭据，我帮你部署。
