# 日常记录软件 · 研发跟进（任务拆解）

| 项 | 内容 |
|----|------|
| 文档版本 | v1.0 |
| 阶段 | 阶段四：研发跟进 |
| 范围 | v1.1 需求拆解（v1 = P1+P2 已交付并通过评审） |
| 关联文档 | `评审-日常记录软件-研发设计评审.md`、`PRD-日常记录软件-P1.md`、`PRD-日常记录软件-P2.md` |
| 最后更新 | 2025 |

---

## 1. 背景与范围

- **v1（已发布就绪）**：P1 核心闭环 + P2 增强，全部功能已实现并通过构建；评审中唯一中风险项（体重同日多值图表重叠）已修复。
- **v1.1（本文拆解）**：来自评审材料第 6.2 节的后续候选 + 工程优化项。
- **不在 v1.1**：云端同步 / 多账户（架构级变更，建议独立大版本，见第 5 节）。

---

## 2. 迭代规划建议

| 迭代 | 目标 | 包含任务 |
|------|------|----------|
| **迭代一（体验增强）** | 补齐高感知功能 | T1 习惯每周目标 · T2 喝水目标线 · T3 浏览器通知 · T4 心情走势 |
| **迭代二（工程优化）** | 性能与长期可维护 | T5 列表分页 · T6 打包代码分割 |

> 排期 / 资源为建议，需与研发负责人确认后定稿（见第 6 节提醒）。

---

## 3. 任务拆解

### T1 习惯「每周 N 次」目标

| 子任务 | 依赖 | 估时 | 负责人角色 | 验收 |
|--------|------|------|------------|------|
| 数据模型：Habit 增 `weeklyTarget?` 字段，Dexie v3 迁移 | 无 | 0.25d | 研发 | 旧习惯 weeklyTarget 为空，向后兼容 |
| 习惯页展示「本周 X/N」进度（按自然周统计打卡去重天数） | T1.1 | 0.5d | 研发 | 周一切换正确，跨周归零 |
| 打卡逻辑不变（仍每日 toggle） | 无 | 0 | 研发 | 行为同 v1 |
| 设置/引导说明文案 | 无 | 0.25d | 产品 | 文案清晰 |

### T2 喝水每日目标线

| 子任务 | 依赖 | 估时 | 负责人角色 | 验收 |
|--------|------|------|------------|------|
| 设置增 `waterTarget?`（默认 2000ml） | 无 | 0.25d | 研发 | 可配置 |
| 喝水页展示进度环 / 目标线（今日总量 vs 目标） | T2.1 | 0.5d | 研发 | 达目标态变化 |
| 统计页可纳入饮水完成度（可选） | T2.1 | 0.25d | 研发 | 图表正确 |

### T3 浏览器 Notification 提醒（应用外可弹）

| 子任务 | 依赖 | 估时 | 负责人角色 | 验收 |
|--------|------|------|------------|------|
| 评估 PWA / Service Worker 方案（是否做 PWA 是基础决策） | **需架构确认** | 0.5d | 研发+产品 | 方案拍板 |
| 接入 Notification API，请求授权，未完成项到点弹系统通知 | T3.1 | 1d | 研发 | 授权后可弹 |
| 保留首页应用内横幅作为未授权兜底 | 无 | 0.25d | 研发 | 兜底可用 |
| 提醒配置项（开关 / 时间） | T3.1 | 0.25d | 研发 | 可关 |

> ⚠️ **阻塞点**：T3 是否做 PWA 是架构级决策，需先确认（见第 5 节）。

### T4 心情周/月情绪走势图

| 子任务 | 依赖 | 估时 | 负责人角色 | 验收 |
|--------|------|------|------------|------|
| 统计页新增「心情趋势」，按周/月聚合平均 mood（1–5） | 无 | 0.75d | 研发 | 曲线随数据更新 |
| 维度切换（周/月）控件 | T4.1 | 0.25d | 研发 | 切换正确 |

### T5 列表分页 / 时间窗（长期数据）

| 子任务 | 依赖 | 估时 | 负责人角色 | 验收 |
|--------|------|------|------------|------|
| 心情 / 打卡 / 待办列表按时间窗或分页加载 | 无 | 0.75d | 研发 | 大数据量不卡顿 |
| （可选）虚拟滚动 | T5.1 | 0.25d | 研发 | 长列表流畅 |

### T6 打包体积优化（代码分割）

| 子任务 | 依赖 | 估时 | 负责人角色 | 验收 |
|--------|------|------|------------|------|
| 路由级 `React.lazy` 拆分页面 | 无 | 0.25d | 研发 | 首屏 JS 下降 |
| Recharts 按需引入 / manualChunks | 无 | 0.25d | 研发 | bundle 拆分生效 |

---

## 4. 依赖关系总览

```
T1 (独立)      T2 (独立)      T4 (独立)
T3 ──依赖──▶ [PWA 架构决策]   ← 阻塞点
T5 (独立)      T6 (独立)

迭代一：T1, T2, T3, T4（T3 取决于 PWA 决策，可先并行做 T1/T2/T4）
迭代二：T5, T6
```

---

## 5. 风险与阻塞

| 项 | 类型 | 说明 / 处理 |
|----|------|-------------|
| T3 是否做 PWA | **架构决策 / 阻塞** | 浏览器通知在 Web 上通常需 Service Worker；是否投入 PWA 化影响 T3 方案与工作量。**需产品+研发负责人确认。** |
| 云端同步 / 多账户 | 范围外（大版本） | 架构级变更（引入后端/账号），不建议塞入 v1.1，单独立项。 |
| 列表性能 | 低 | v1 数据量小无感；T5 在 v1.1 迭代二处理。 |
| 打包体积 | 低 | T6 处理，非阻塞。 |

---

## 6. 进度摘要模板（供每日 / 每周同步）

**每日进度摘要**
```
日期：____
· 本日完成：T__（子任务__）；T__ 进行中（__%）
· 阻塞：____（待 __ 确认）
· 风险：____
```

**每周研发进度摘要**
```
周期：____
· 迭代一完成度：__/4 任务
· 已上线/待验：____
· 阻塞与依赖：____
· 下周计划：____
```

---

## 7. 流转提醒

1. 将本文任务拆解 **上传项目资料库**，并附上 PRD 与评审结论。
2. 把各任务**拆成事项**分配给研发同学，并：
   - 附上对应文档交付物（PRD / 评审材料）
   - 添加关注人（产品 / 设计 / 测试）
3. 阻塞项 **T3（PWA 决策）** 与排期/资源，提醒找对应负责人确认。
4. 研发完成后进入**阶段五：测试验收**（基于 PRD 生成测试用例）。

> 当前 `项目资料库（tdrive）` 与 `事项（wb-issues）` 连接器未连接，上传 / 建事项需连上后由我执行。

---

## 8. 实现状态（v1.1 迭代一）

| 任务 | 状态 | 实现要点 |
|------|------|----------|
| T1 习惯每周目标 | ✅ 已实现 | Habit 增 `weeklyTarget`；习惯页展示「本周 X/N」；打卡逻辑不变 |
| T2 喝水目标线 | ✅ 已实现 | 设置增 `waterTarget`；喝水页进度条 + 还差 X ml；统计可纳入 |
| T3 浏览器通知 | ✅ 已实现 | 设置开关 + 授权；页面打开时每 5 分钟检查，当天仅提醒一次（系统通知 + 首页横幅兜底）|
| T4 心情走势 | ✅ 已实现 | 统计页新增心情趋势，周/月聚合平均情绪（1–5）|

> 说明：T3 的「浏览器通知」在纯 Web 下需页面处于打开状态才会触发（受 Notification API 限制）；真正的离开网页后台推送需 Service Worker / PWA 化，留作后续。迭代二（T5 分页 / T6 代码分割）待排期。

---

*本文件为阶段四研发跟进产出，将 v1.1 需求拆解为可执行研发任务。*
