# 日常记录软件 · 产品策划案（立项稿）

> 状态：需求规划阶段（已评审通过）｜最后更新：2025
> 协同角色：产品（你）、设计、研发、测试

---

## 1. 产品定位

**一句话定位**：面向学生 / 职场新人的轻量「日常记录 + 自我管理」Web 工具，把分散在多个 App 里的体重、待办、番茄钟等需求聚合到一个入口。

**定位三角**

| 维度 | 结论 |
|------|------|
| 目标用户 | 学生、职场新人（自我管理意愿强、工具预算低、怕复杂） |
| 核心场景 | 日常随手记录 + 学习 / 工作效率管理 |
| 差异化 | 多模块聚合 · 本地优先（隐私、免登录）· 架构可平滑演进到移动端 |

**痛点假设**（待用户访谈验证）：用户现在需用 A 记体重、B 管待办、C 计时番茄钟，切换成本高、数据割裂；想要一个轻、快、全的聚合入口。

---

## 2. 目标与成功指标

**产品目标**：验证「聚合式日常记录」对用户日常管理效率与留存的价值。

**北极星指标（建议）**：周活跃天数（用户每周打开并记录的的天数）。

**核心量化指标**

| 指标 | 口径 | 说明 |
|------|------|------|
| 体重记录留存率 | 连续记录 ≥7 天用户占比 | 衡量核心习惯养成 |
| 待办完成率 | 已完成 / 已创建 | 衡量效率价值 |
| 日均番茄数 / 周专注时长 | 番茄钟完成数 × 单时长 | 衡量专注模块价值 |
| 习惯打卡连续天数 | 各习惯最大连续 streak | 衡量留存钩子 |

---

## 3. 目标用户与场景

| 用户画像 | 典型痛点 | 高频场景 |
|----------|----------|----------|
| 减脂期学生 | 体重记录零散、看不到趋势 | 每天称完顺手记，看曲线 |
| 备考职场新人 | 待办多、易拖延 | 列待办 → 开番茄钟专注 → 看完成率 |
| 自律养成者 | 习惯难坚持 | 每日打卡，连续天数激励 |

---

## 4. 功能清单（MVP · 较完整版本）

> ⚠️ 你选的「较完整版本」实际含 9 个模块，对首版偏重。**建议分两期**（见第 11 节），先把核心闭环做透。

| # | 模块 | 功能要点 | 分期 | 优先级 |
|---|------|----------|------|--------|
| 1 | 体重记录 | 录入体重（含日期/备注）、趋势曲线、目标线 | P1 | 高 |
| 2 | 每日待办 | 增删改、完成、今日/全部、分类标签 | P1 | 高 |
| 3 | 番茄钟 | 计时（默认 25/5）、**绑定具体待办**、累计专注时长 | P1 | 高 |
| 4 | 日历视图 | 月/周视图聚合待办、记录、打卡、心情 | P1 | 中 |
| 5 | 数据统计图表 | 体重曲线、专注时长、任务完成率可视化 | P1 | 中 |
| 6 | 习惯打卡 | 自定义习惯、每日打卡、连续天数激励 | P2 | 中 |
| 7 | 心情/日记 | 简短心情 + 文字，按日期回溯 | P2 | 低 |
| 8 | 喝水/饮食记录 | 轻量记录，与体重形成健康维度 | P2 | 低 |
| 9 | 智能提醒 | **仅应用内提醒**（打开网页时弹），任务/喝水/打卡提醒 | P2 | 低 |
| — | 全局 | 本地存储、设置、数据导出（JSON/CSV）、首次引导 | P1 | 高 |

---

## 5. 信息架构（页面结构）

```
App
├─ 首页 Dashboard（今日概览：最新体重 / 今日待办 / 进行中番茄 / 打卡 / 喝水）
├─ 待办 Todo
├─ 番茄钟 Focus（含计时器 + 绑定任务选择）
├─ 体重 Weight（录入 + 趋势）
├─ 习惯 Habits（P2）
├─ 记录 Journal（心情/喝水/饮食，P2）
├─ 日历 Calendar
├─ 统计 Stats
└─ 设置 Settings（导出、提醒开关、关于）
```

导航：左侧栏（宽屏）/ 底部 Tab（窄屏，为移动端预留）。

---

## 6. 核心用户流程

1. **记录体重**：首页/体重页 → 输入数值+日期 → 保存 → 曲线更新。
2. **创建并完成待办**：待办页 → 新建（标题/日期/标签）→ 勾选完成 → 完成率+1。
3. **番茄钟（绑定任务）**：Focus 页 → 选待办 → 开始 25min → 结束自动记一次专注 → 该任务累计时长+1。
4. **习惯打卡**：Habits 页 → 点当日打卡 → streak +1，破零/连击反馈。
5. **查看统计**：Stats 页 → 切换体重/专注/完成率维度 → 看趋势与目标达成。

---

## 7. 数据模型（本地存储）

采用 **IndexedDB**（经 Dexie 封装）存结构化数据，便于统计查询；明确数据层，方便后期接云同步。

| 实体 | 关键字段（示意） |
|------|------------------|
| WeightRecord | id, date, value, note, createdAt |
| Todo | id, title, date, tag, done, createdAt |
| PomodoroSession | id, todoId, start, end, duration, done |
| Habit | id, name, color, createdAt |
| HabitCheckIn | id, habitId, date |
| MoodEntry | id, date, mood, text |
| IntakeRecord | id, date, type(water/diet), amount, note |
| Reminder | id, type, time, content, enabled |
| UserSettings | theme, pomodoroLen, breakLen, exportFormat |

---

## 8. 技术选型（Web 优先 · 可演进移动端）

| 层 | 推荐 | 理由 |
|----|------|------|
| 框架 | **React + Vite + TypeScript**（或 Vue3） | 生态成熟、组件化、利于后续 React Native 复用逻辑 |
| 样式 | Tailwind CSS | 快速、统一视觉 |
| 状态 | Zustand（React）/ Pinia（Vue） | 轻量 |
| 存储 | **Dexie（IndexedDB）** | 结构化、可查询、本地优先 |
| 图表 | ECharts / Chart.js | 体重曲线与统计 |
| 形态建议 | **做成 PWA** | 可「添加到主屏幕」、离线可用，为移动端铺路 |

**数据策略**：本地优先，无账号、无后端；提供 **JSON/CSV 导出**做备份。后期如需多端同步，再叠加云端账号层（数据层已隔离，改动可控）。

---

## 9. 非功能需求

- **兼容性**：现代浏览器（Chrome/Edge/Safari/Firefox 近两版）。
- **性能**：本地读取，首屏秒开；列表/统计按需加载。
- **隐私**：数据不出本机；导出文件归用户所有。
- **可访问性**：基础对比度与键盘可达性。
- **响应式**：宽屏侧栏、窄屏底部 Tab，适配手机浏览器。

---

## 10. 埋点 / 数据指标

| 事件 | 用途 |
|------|------|
| 模块访问（weight/todo/focus/...） | 功能使用分布 |
| 记录提交（体重/打卡/心情/喝水） | 活跃度 |
| 番茄完成（含绑定任务） | 专注价值 |
| 设置导出 | 备份需求强度 |

> 本地埋点存本地，用于产品自评估；若后续上线分析后台再对接。

---

## 11. 里程碑 / 排期建议（粗）

| 阶段 | 范围 | 目标 | 估时 |
|------|------|------|------|
| **P1 核心闭环** | 体重+待办+番茄钟+日历+统计+全局(存储/导出/引导) | 跑通记录→统计闭环，验证核心价值 | 2–3 周 |
| **P2 增强** | 习惯打卡+心情/日记+喝水/饮食+应用内提醒 | 提升留存与粘性 | 2–3 周 |

> 排期/资源为建议，需与研发负责人确认后定稿。

---

## 12. 待确认问题 / 风险

1. **分期确认**：是否接受 P1/P2 分期（先验证核心，再上扩展）？还是坚持一次性全上？
2. **提醒局限**：Web 仅应用内提醒，离开网页即失效——是否可接受？还是需浏览器通知（Notification API，需授权）？
3. **数据备份**：是否需要导入/导出、或云同步优先级？
4. **多用户**：是否涉及家庭成员共享/多账户？（当前按单人本地设计）
5. **移动端时机**：PWA 是否满足「后期移动端」预期，还是计划原生重写？
6. **视觉风格**：希望清新简约 / 数据仪表盘风 / 拟物可爱？影响设计方向。

---

## 13. 下一步流转（规划阶段收尾）

- ✅ 将本策划案**上传项目资料库**，形成统一上下文。
- ✅ 把确定要推进的需求**创建为事项**，分配给对应负责人，并邀请设计/研发/测试关注。
- ➡️ 进入**阶段二：PRD 撰写**（按 EARS 原则逐条写需求、补边界/异常/埋点），再发起评审。

---

*本文件为规划阶段产出，后续可随讨论持续迭代。标注 ⚠️ / 待确认 的条目请在评审前拍板。*
