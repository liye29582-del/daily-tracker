# 日常记录软件 · 研发 & 设计评审材料

| 项 | 内容 |
|----|------|
| 文档版本 | v1.0（评审稿） |
| 阶段 | 阶段三：设计与研发评审 |
| 范围 | P1 + P2 全量走查（v1 上线范围） |
| 关联文档 | `PRD-日常记录软件-P1.md`、`PRD-日常记录软件-P2.md`、`日常记录软件-策划案.md` |
| 最后更新 | 2025 |

---

## 1. 评审结论速览（已确认）

| 议题 | 结论 |
|------|------|
| 视觉风格 | **清新简约**：浅色留白、圆角卡片、克制品牌色（当前实现已采用） |
| 评审聚焦 | **全量走查 P1 + P2** |
| v1 上线范围 | **P1 + P2 一起作为 v1** |
| 已知技术边界 | **全部接受为已知边界**（番茄钟刷新重置 / 提醒仅应用内 / 无云端同步） |

---

## 2. 评审议题清单（评审会议程）

1. 需求覆盖核对：PRD（P1+P2）vs 已实现功能，逐模块确认。
2. 视觉风格与一致性：清新简约落地情况、响应式、空态/异常态。
3. 交互边界与异常：打卡 toggle、刷新重置、空输入拦截等边界是否符合 PRD。
4. 技术架构与依赖：纯前端 + IndexedDB，无后端，识别风险。
5. 上线范围与已知边界：确认 v1 = P1+P2，已知边界写入发布说明。
6. 后续优化候选：PRD 待确认项的归属（v1.1 / 后续）。

---

## 3. 设计评审

### 3.1 页面清单（9 页）

| 页面 | 核心状态 | 交互边界 |
|------|----------|----------|
| 首页 Dashboard | 有未完成项→提醒横幅；无→鼓励语 | 习惯卡显示 today 已打卡/总数 |
| 体重 Weight | 记录数 ≥2→趋势图；<2→仅列表 | 目标线为可选（设置开启）|
| 待办 Todo | 今日 / 全部 | 勾选切换完成；空标题拦截 |
| 番茄钟 Focus | 专注 / 休息；running / paused | 绑定今日未完成任务；刷新重置 |
| 习惯 Habits | 已打卡 / 未打卡 | 打卡 toggle；删除连带打卡记录 |
| 记录 Journal | 心情 / 喝水 / 饮食 三 Tab | 心情可多条；饮食空文字拦截 |
| 日历 Calendar | 月视图 + 选中日明细 | 5 色标记（体重/待办/番茄/习惯/心情）|
| 统计 Stats | 汇总卡 + 两图 | 体重<2 条占位；近 14 天专注 |
| 设置 Settings | 时长 / 目标 / 导出 | 修改即时生效 |

### 3.2 设计验收点

- [ ] **视觉一致性**：圆角卡片、品牌蓝主色、统一间距与字号。
- [ ] **响应式**：宽屏左侧栏；窄屏底部 Tab（横向滚动，≥9 入口不溢出）。
- [ ] **空态**：各列表空态文案完整（体重/待办/习惯/心情/饮食）。
- [ ] **错误内联提示**：空名称、空饮食、非法体重均内联拦截，不弹窗阻断。
- [ ] **反馈即时性**：增删改后列表/图表即时刷新，无整页重载。
- [ ] **可访问性**：基础对比度达标；主要操作可键盘可达。

---

## 4. 研发评审

### 4.1 技术依赖与架构

| 层 | 选型 | 说明 |
|----|------|------|
| 框架 | React 18 + Vite + TS | 纯前端 SPA，无服务端 |
| 样式 | Tailwind CSS | 清新简约落地 |
| 状态 | Zustand | 设置 + 持久化番茄钟 |
| 存储 | Dexie（IndexedDB） | 本地优先，v1→v2 已含迁移 |
| 图表 | Recharts | 体重趋势 + 专注柱状 |
| 形态 | Web（PWA 化留待后续） | 移动端靠响应式 + 横向 Tab |

**关键依赖结论**：无后端、无第三方账号/SDK，部署为零运维；多端同步能力缺失（架构性，v1 接受）。

### 4.2 数据结构

- 实体：weightRecords / todos / pomodoroSessions / habits / habitCheckIns / moods / intakes / settings（共 8 表）。
- 关系：PomodoroSession.todoId → Todo；HabitCheckIn.habitId → Habit（均为可选/外键逻辑层维护）。
- 迁移：Dexie `version(2)` 新增 4 表，旧库自动升级。

### 4.3 实现风险

| 风险 | 等级 | 说明 / 建议 |
|------|------|-------------|
| 体重同日多值 → 趋势图重叠 | 中 | 当前图表按 date 取点，同日多条会生成相同 x 的多点，Recharts 可能重叠。**建议**：趋势图按日聚合取当日末值。 |
| 长期数据未分页 | 低 | 心情/打卡随时间增长，列表未分页，长期渲染压力。**建议**：v1.1 按时间窗或分页。 |
| 打包体积 ~690KB | 低 | Recharts 较重，首屏略大。**建议**：按需引入 / 路由级代码分割（非阻塞）。 |
| IndexedDB 不可用 | 低 | 已做首屏提示（FR-005），可接受。 |
| 番茄钟刷新重置 | 已知边界 | v1 接受，v1.1 可加后台保活/本地暂存。 |
| 提醒仅应用内 | 已知边界 | v1 接受，后续可接浏览器 Notification。 |
| 无云端同步 | 架构性 | v1 接受；数据靠「设置→导出」备份。 |

---

## 5. 风险清单（汇总）

- **高**：无（核心闭环已实现并通过构建）。
- **中**：体重同日多值图表重叠（需在 v1 前修复或明确为已知行为）。
- **低/已知**：刷新重置、提醒应用内、无同步、体积、长期分页。

> 评审建议：将「体重同日多值聚合」列为 **v1 上线前修复项**（小改动、影响核心图表可信度）；其余低/已知项写入发布说明。

---

## 6. 待确认问题 & 结论

### 6.1 本轮已拍板（见第 1 节）
视觉风格、评审范围、上线范围、技术边界——均已确认。

### 6.2 PRD 遗留待确认项归属

| 待确认项 | 来源 | 归属建议 |
|----------|------|----------|
| 习惯「每周 N 次」目标 | P2 PRD Q1 | 留待 v1.1 |
| 浏览器 Notification 提醒 | P1/P2 PRD | 留待 v1.1（当前应用内已接受）|
| 心情周/月情绪走势图 | P2 PRD Q3 | 留待 v1.1（统计增强）|
| 喝水每日目标线（如 2000ml）| P2 PRD Q4 | 留待 v1.1 |
| 体重同日多值取值口径 | P1 PRD Q3 | **v1 前修复**（趋势图聚合取末值）|
| 导出范围扩至全量 CSV | P1 PRD Q4 | 留待 v1.1（当前 JSON 全量 + 体重 CSV）|

---

## 7. 流转建议

1. 将本评审材料与 P1/P2 PRD **上传项目资料库**，形成统一上下文。
2. 把以下待办**拆成事项**分配负责人并加关注人：
   - 【v1 前修复】体重同日多值趋势图聚合（研发）
   - 【v1.1】习惯每周目标 / 浏览器通知 / 心情走势 / 喝水目标线（产品+研发）
   - 【v1.1】列表分页、代码分割优化（研发）
3. 评审结论沉淀资料库；后续进入**阶段四：研发跟进**与**阶段五：测试验收**。

> 当前 `项目资料库（tdrive）` 与 `事项（wb-issues）` 连接器未连接，上传/建事项暂需你连上后由我执行。

---

## 8. 修复记录

| 日期 | 项 | 处理 |
|------|----|------|
| 2025 | 体重同日多值趋势图重叠（原风险「中」） | **已修复**：新增 `aggregateWeightByDate`，趋势图按日聚合取当日最新一条；`Weight.tsx` 与 `Stats.tsx` 同步应用，已通过构建。 |

> 该中风险项已闭环，剩余风险均为低/已知边界，v1 可发布。

---

*本材料供设计、研发、测试评审使用。评审通过后即可进入研发跟进与测试验收。*
